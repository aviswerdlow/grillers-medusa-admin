# Reviewed checkout and original-order evidence — #368

Tracking: [strategy #368](https://github.com/aviswerdlow/grillers-pride-strategy/issues/368). This candidate connects the immutable ledger to customer saved-card/invoice checkout and staff phone-card completion. Stage it with enforcement off, then activate with the matching storefront review UI after rehearsal. It remains an **unmerged source candidate**: native inventory/completion/provider acceptance, staff amendments, analytics consumers and QuickBooks corrections are unfinished. Do not close #368 or enable required enforcement independently.

## Independent deployment and defaults (#372)

`GP_ORDER_REVIEW_ENFORCEMENT=off|required` defaults to `off` (empty/unset also off; an unrecognized configured value fails closed as required). With current storefront main and default off, customer saved-card and approved invoice placement keep accepting legacy `consent_version`/`consent_text`; no review/calendar/Terms fetch or order-promise database lookup is needed by this new review gate. Native completion delegates to Medusa's original handler and response shape, retaining existing inventory, staff, calendar, receipt, payment and final-charge guards. This flag does not bypass those separate controls. Existing main's invoice-term fallback is retained only for unreviewed orders during rollout; actual approved account terms remain an activation prerequisite.

`GET /store/carts/:id/order-review` reports `off|required` after ownership verification. `POST action=review` returns 404 when off on an unreviewed cart. An explicit review/request ID or an existing accepted snapshot keeps full review/binding enforcement even on rollback. Native copy validates again, and a promise copied concurrently must still bind before finalization. Required mode blocks unreviewed native/admin creation. Default off leaves native admin creation subject to the existing staff checks.

Frontend PR60 keeps sending the legacy consent fields and falls back only on 404, after fresh cart and capability reads; 403/409/503, required/malformed capability, completed/missing carts and accepted promises do not downgrade. There is no frontend flag. It rechecks compatibility before Stripe work. Activation needs reviewed calendar/receipt/shipping inputs, matching frontend, published Terms, DB backup/migration readback and one integrated native/provider rehearsal. Other #372 staff/contact defects inherited by this stack remain merge gates; this change is not permission to merge the whole stack yet.

On 2026-09-21, a read-only Strapi query returned one published `terms-of-sale`, updated 2026-06-17T00:10:53.315Z, revision `strapi-sale-terms:9e12ad7d2cf6089464fddb5fdd46a1140a9efb3191f5872850fcc9081664dfbf`. The current storefront Terms URL returned 200 and contained the catch-weight disclosure. No publication was necessary or performed. New consent version `catch-weight-final-charge-2026-09-21` explicitly says per-pound checkout prices are estimates and the final food total reflects actual packed weight, then retains the save-card/charge-when-ready authorization. Backend review and frontend fallback use the same text. The published substitution paragraph and blanket 72-hour temperature claim still need reconciliation with #359 and #357/#374 before activation; publication is not policy approval.

## Customer and staff behavior (required mode)

The active payment step shows a server-derived review before placement: items and quantities, fixed/per-pound price basis, estimated food weight and total, shipping, tax/discounts included, billing and delivery addresses, receipt email/phone, actual service/date/window, approved invoice terms or card consent, and canonical legal links. Direct edit links return to the existing cart, address, date and profile screens. Changes and expiry disable placement until refreshed. The separate legacy review component is not the integration point.

Customer card checkout keeps the existing save-card/charge-when-packed flow. Approved invoice checkout needs the account's actual terms; there is no Net 10 fallback. Staff immediate collection needs verified `collect_card_now` authority and consent. It refuses catch-weight lines: use the existing customer final-charge checkout or send-checkout-link path instead of charging an estimate as a fixed final payment. Native Admin direct/draft order creation and conversion are denied, including for operators, because they bypass the accepted review.

An uncertain response is not a reason to collect another payment. Customer status recovery can replay only an already completed cart with its original review/request identifiers. Staff keep their confirmed payment reference and expose a completion-only retry. This is source behavior, not yet a native/provider recovery receipt. Browser reload persistence and every recovery scenario must be checked in the integrated rehearsal.

## API and authority

`POST /store/carts/:id/order-review` accepts only `action: review | accept | recover`, `payment_mode: card | card_at_placement | invoice`, a UUID `request_id`, optional `review_id`, and nullable boolean `analytics_consent`. The browser cannot submit a promise, price, ListID, terms document, receipt destination or test flag. `recover` refuses an uncompleted cart before calling native completion.

In required mode, `POST /store/grillers/checkout/place-order` requires the review and request identifiers for customer saved-card or approved invoice placement. `POST /store/carts/:id/complete` is the reviewed staff immediate-payment adapter, carrying identifiers in `x-gp-order-review-id` and `x-gp-order-request-id`; native SDK body validation remains intact. It retains the native result envelope.

The authenticated customer must own the cart. Staff authority comes only from the existing middleware-verified signed cart/customer handoff and current staff session. The trusted adapter rechecks the actor's capability, session epoch and access generation at native copy. Staff browser analytics consent is stored as unknown, never attributed to the customer. The public review allowlist omits accounting IDs, internal packing/costs, private receipt IDs and experiment data. Public cart responses redact the server-owned snapshot pointer.

## Trusted data and timing

`src/lib/order-review-checkout.ts` derives the full promise from the exact cart the native workflow copies, current server catalog records, an owned immutable receipt snapshot, accepted calendar and shipping contracts, and published sale terms. Cart-line injections cannot supply pricing mode, food weight, name or QBD ListID. It stamps the accepted server values onto copied line metadata. RM items, absent identity/pricing/contact/terms and changed inputs fail closed. Explicit zero totals remain zero. Pie-space allowances never become food charge weights.

The Strapi published Terms of Sale query matches the storefront legal source fields. Its document and SHA-256 revision are archived. This establishes the source revision, not Peter/Avi's legal approval or proof that every editorial block was rendered. Fetching is bounded to five seconds; an unavailable or incomplete source blocks a new review. A read-only diagnostic returned the expected published source after the original two-second loader failed. The failure's exact cause was not proven; candidate availability and latency remain rehearsal gates. No CMS write occurred.

Semantic comparison omits changing capture/acceptance timestamps and decrypts the signed shipping price instead of comparing randomized ciphertext. All four fulfillment modes are preserved; regional pickup includes the configured city/state. Reviews expire at the earliest of 15 minutes, calendar quote/cutoff, signed shipping-token expiry and shipping-policy validity. Unknown experiment versions, analytics consent and Stripe mode remain null; they are not verified measurement data.

## Lock, completion and original evidence

Review issue/acceptance, metadata stamps and shipping-heal writes use the same cart-ID lock key as native Medusa cart mutation/completion. Native completion acquires that lock itself; do not invoke native cart workflows inside a review lock callback. The existing single `completeCartWorkflow.hooks.validate` callback composes receipt, calendar, shipping and review validation against the actual native copy source. It does not register a second competing hook. Actual native stock/reservation atomicity remains #312/#364 work.

`src/lib/order-promise.ts` and the catch-weight migration own three append-only tables: issued reviews, accepted snapshots and successful native order bindings. Acceptance compares the normalized promise, records a monotonic revision and updates the cart pointer transactionally. Replays retain the original expiry; an idempotency key cannot change meaning or revive a superseded acceptance. Database triggers reject UPDATE/DELETE. Historical orders are not backfilled from current state.

`completeReviewedCart` keeps the real in-process `completeCartWorkflow.run()` return. `bindOrderPromise` requires the matching `complete-cart` transaction/input, successful done state, order/run IDs, durable completed cart/order/link, owner and matching pointers. An order ID, client success flag or event payload is insufficient. It then initializes packing from the bound original. A binding/finalization interruption returns an explicit recovery error without initiating another provider action. Replaying a completed cart uses its original acceptance, not current profile/catalog data.

The existing order-placed packing subscriber defers a reviewed order until binding exists. `acceptedFinalizationSource` maps the native order-line IDs to immutable accepted cart lines and uses original identities, food-pricing basis and placement totals. Changed quantity/product/variant/line membership requires the unfinished amendment workflow. Legacy orders retain their old path. Packing's later actual weight/final charge remains distinct from the original placement estimate.

Medusa 2.10.3 queues `order.placed` before its final authorization steps and clears grouped events during compensation. This source observation does **not** show failed orders escaping as events. Only a real integrated rehearsal can establish native event/recovery ordering. Stripe, Medusa, allocation, bridge, QBD and messages each require their own receipt.

## Remaining work and closure dependencies

#368 hard closure dependencies remain **#314, #318, #320, #329, #331, #359, #362, #364 and #366**. The #365 primary-contact work is a phase handoff; this candidate does not resolve its stopped fixture. Frontend composition includes receipt PR56 and shipping PR55 on the homepage/calendar/incoming/staff candidate chain. Backend PR40 composes receipt PR37 and incoming/staff PR38. Preserve those prerequisite branches until the integrator records exact merged SHAs.

1. **Native/runtime acceptance — #312/#332/#364.** Use the integrated candidate and required test identities for customer saved/new card, invoice and staff orders. Cover expired/changed review, cart edits and owner/staff revocation races, completion failure, post-completion binding loss and same-order recovery. Check actual incoming allocation/reservations, copied line metadata and original amount/contact retention. The stopped #312 fixture was not rerun.
2. **Staff corrections — #359/#368/#370.** Peter's description answers require online additions/removals before packing, actual staff attribution and substitution opt-in. Complete the remaining timezone/out-of-town/higher-price permission questions. Add consequence preview, revision/capability/reason/approval checks, in-progress-pick reconciliation/physical removal confirmation and separately audited amendments. Recheck calendar, stock, tax, promotions, final-charge/invoice exposure and packed/cutoff races. Preserve the original acceptance; profile edits cannot redirect an existing order.
3. **Accounting — #314/#317/#320.** `update_sales_order_items` is not a certified bridge operation. Verify TxnID/EditSequence and the approved correction mapping before amendments. Retain immutable outbox handling and separate Stripe/Medusa/bridge/QBD receipts.
4. **Evidence consumers — #335/#336/#367/#369.** The privacy-safe `orderPromiseAnalytics` helper now has a [dedicated original-order reader](original-order-analytics-reader.md) and paired parity-client source candidate. The publisher, destination/communications continuity and shipping-ledger consumers still need integration. Preserve one stable order ID across placement/finalization, idempotency and experiment/consent semantics. Missing originals are unavailable, never zero/current/final amounts. Do not send a private promise into events, logs, customer responses or alerts.
5. **Controlled rollout.** Apply migrations only to the authorized integrated candidate/rehearsal. Rollback retains the evidence tables. Production deployment, money actions, messages, QBD/Stripe switches and domain changes remain separate authorized handovers. The frontend operations guide describes the candidate and its limits; deployment does not establish launch approval.

## Verification scope

Focused units cover trusted catalog authority, zero/missing totals, semantic comparison, exact-cart changes, invoice terms, privacy, current staff revocation, immutable finalization inputs, actual native-return binding order, uncertain completion, route/body guards and Admin bypass denial. Frontend units cover loading, editable review, expiry, stale/out-of-order responses, payment prerequisites and completion-only staff retry. Synthetic desktop/mobile inspection renders the actual review component with mocked server actions; it is not a Next/Medusa/provider end-to-end run.

The PostgreSQL CI suite executes the actual ledger migration and transactions with isolated cart/order/link fixtures: concurrency, staleness, rollback, forgery, append-only storage and original amount/contact retention. It does not execute native Medusa completion or providers. CI also runs inherited accounting, receipt and incoming-stock checks plus TypeScript. Record the exact candidate SHA and run receipt in #368; old ledger-only CI is not proof for this integration.

For an isolated local ledger test use `ORDER_PROMISE_TEST_DATABASE_URL` pointing to loopback database `gp_order_promises` (CI uses `gp_launch`), or `ORDER_PROMISE_TEST_PG_SOCKET` for port 55468/user `gp_order_promise_test`. Run `TEST_TYPE=integration:order-promise yarn jest --runInBand`. This suite never reads `DATABASE_URL`.

## Migration recovery and release backup (#372)

Before a migrating merge, record a fresh database backup, the restore target/procedure and the exact candidate SHA. The accepted-order migration retains existing tables and installs only missing immutable-evidence triggers. Reapplying it preserves accepted reviews, snapshots and order bindings without temporarily dropping their guards. The PostgreSQL migration-replay tests cover populated replay and a missing trigger; backup custody and a controlled Railway rehearsal remain separate operator evidence. Do not delete accepted evidence or use a destructive down migration to recover.
