# Native reservation candidate for strategy #312 — held

This draft stacks on #340 and preserves the existing staff-authority and public-catalog contracts. It does not write production stock. Peter's approved physical/QBD stock export, location and exception dispositions are still required for a separate baseline operation.

The candidate removes the unmanaged/backorder `999999` assumption and cached-QBD fallback. It uses mapped native inventory levels and refuses missing baselines. It compares advisory allocations to native reservation rows by line, item and location, so mirrored demand is not deducted twice. A lead-time estimate alone no longer promises future stock; #364 must supply confirmed incoming reservations. Order-row locking makes advisory placement and whole-order release replay-safe. Native cart/payment guards refuse unverified stock.

The dedicated loopback `gp_inventory_fixture` database and Redis fixture now runs installed Medusa inventory and native reservation-step handlers. Local results on September 22: 38/38 focused unit tests and TypeScript pass. The native fixture passes last-unit concurrency across independent Redis clients, advisory placement replay, decimal component units and unmanaged checkout refusal (5/6 checks). **Cancellation release fails:** after the installed compensation handler deletes the reservation row, the Medusa inventory level still reports `reserved_quantity=1` instead of `0`. The targeted retry reproduced that state. The CI workflow includes this gate so the draft cannot appear green while release is broken. The previous schema setup failure is resolved by using a dedicated disposable database.

Do not merge or deploy. The native reserved-counter defect needs a separate diagnosis before another test iteration. Explicit pre-fulfillment partial refund release remains unwired; Stripe refund success is not inventory release. #364 and #323 remain downstream and were not started from this unverified candidate.
